define('settings_local',
    ['core/settings'],
    function(settings) {
    settings._extend({
        // Remove this to have data fetched from CDN rather than API.
        api_cdn_whitelist: {},
        api_url: 'https://marketplace-dev.allizom.org',
        media_url: 'https://marketplace-dev-cdn.allizom.org/media/'
    });
});
