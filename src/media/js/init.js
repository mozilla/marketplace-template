/*
    Put code needed to initialize the app.
*/
define('init',
    ['core/init', 'core/log', 'helpers_local', 'routes', 'settings_app'],
    function(init, log, helpers_local, routes, settingsApp) {
});
